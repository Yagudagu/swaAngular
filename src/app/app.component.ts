import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<a href="/login">Login with more words, to know a change</a>`,
})
export class AppComponent {
  value = 'World';
}
